\chapter{Development of the new FASER Pre-shower detector (37 pages)}
	
After an initial prototype developed in 2020 and a second iteration in 2022 featuring the pre-production FASER Pre-Shower ASIC, the full batch of production ASICs was delivered to the cleanrooms of the University of Geneva in early July 2024, after thinning and dicing. A few wafers had already undergone thinning and dicing a couple of months earlier to initiate ASIC qualification in advance. From that point on, a "race against the clock" began: a total of 1,152 ASICs had to be tested in order to select the best 432 candidates for integration into the 72 modules required to assemble six detector planes. The qualification of ASICs, the assembly and testing of modules, and the construction and validation of detector planes had to proceed in parallel to optimise the use of time. Key milestones also needed to be met during two scheduled testbeam campaigns held in late July and mid-October 2024. Installation of the final Pre-Shower detector was planned for early February 2025, in preparation for first beam from ATLAS collisions in May 2025.\\

This chapter presents the full qualification procedure for the ASICs and modules, starting with the probe station measurements used to classify ASICs into quality categories based on their performance characteristics. The assembly process of the modules is of particular importance, as its quality can significantly impact both the mechanical integrity and the electrical performance of the integrated ASICs. To be eligible for integration into a detector plane, each module must meet a series of qualification criteria and pass extensive performance tests. Once validated, the selected modules are assembled into planes, with careful placement to account for specific pixel matrix features present in some ASICs. \\

In parallel with module qualification, development of the Trigger and Data Acquisition (TDAQ) software also progressed. The modules are controlled and read out through the PPB and LB, which can be interfaced either via a graphical user interface (GUI), useful for basic configuration and scripting test procedures, or via a fully custom software stack that directly communicates with the FPGA on the LB through lower-level protocols. While the GUI offers ease of use, it suffers from performance limitations. The custom TDAQ software, although more complex to implement, delivers significantly improved efficiency and will be the interface through which the Pre-Shower detector is operated within the FASER experiment. This TDAQ must ultimately integrate seamlessly into the full FASER readout system. \\

Finally, two testbeam campaigns, held approximately two and a half months apart, were conducted to validate the full system, including modules, planes, PPB and LB boards, the Pre-Shower Interlock and Monitoring (PIM), and the higher-level readout board, the PRB. Even though the complete six-plane configuration was not tested, the campaigns used configurations closely mimicking the final setup. These tests were critical not only to assess the detector's performance, but also to stress-test the entire system and identify subtle issues in advance of installation. 
	
	
	
	
	
	\clearpage
	\section{Detector qualification and commissioning \textcolor{blue}{ 19 pages}}
	The successful deployment of the FASER Pre-Shower detector relies not only on the design and fabrication of its components but also on a rigorous qualification and integration procedure. This section provides a detailed description of the processes undertaken to ensure that each detector element meets the stringent performance and reliability standards required. Particular emphasis is placed on the methodologies used to characterise and validate the detector modules and planes, addressing both the electrical and mechanical aspects of assembly.

	Given the scale of the system and tight scheduling constraints, a systematic approach was adopted to streamline the assembly, testing, and calibration workflows. This included the establishment of robust module assembly procedures, refined through multiple iterations, the definition of optimal front-end working points, and the development of characterisation routines to assess the performance of each ASIC within its module. The qualification process also serves a calibration role, finalising the definition of each ASIC's operating conditions, which ultimately determine its performance.

	Plane assembly proceeds only once the modules have passed the aforementioned qualification and calibration procedures. The various performance metrics are then used to inform a careful placement strategy for the modules within each plane.

	
	
		\subsection{ASIC qualification}
		Before being selected for assembly in a module, the ASICs have to undergo a series of test in order to confirm their electrical properties and make a very fast though meaningful quantification of their operation conditions. Indeed once assembled in a module, a poor performance in terms of electrical stability and operational conditions of an ASIC could compromise the integrity and performance of an entire module hence making it a six-ASICs loss instead of a single ASIC loss if a careful selection isn't made. 
		
		Pre-assembly test were performed using a probe station, a device able to make electrical connection on the pads of an ASIC through the use of needles, carefully positioned above the pads' position and carefully brought down until good pressure, hence electrical contact, is made. Usually a probe station is designed to operate with a small number of needle ($\mathcal{O}(1)$) to test small prototypes for which few contacts are required. 
		\begin{figure}[h]
			\centering
			\includegraphics[width=0.85\textwidth]{files/ProbeCard}
			\caption{Top view of the probe card and its needles mounted in the probe station for testing and grading of the ASICs. The ASIC is positioned below and held in position by the vacuum plate. The needles are not yet in contact with the ASIC but are already aligned.}
			\label{im:ProbeCard}
		\end{figure}
		
		For larger ASICs such as the FASER Pre-Shower production ASIC, many more needles are required to make contact with the $\mathcal{O}(100)$ pads. For such specific applications, a custom probe-card, implementing a custom number of needles on a PCB can ne used as an interface between the ASIC and the power supplies and readout channels required for testing. Figure \ref{im:} shows the probe-card and its needles positioned on of top the ASIC's pads in the probe station. 
		While with very few needles, the alignement in all three spatial direction si performed one by one, here the alignement is constrained by two factors: the positioning and alignement of the needles on the probe card themselves; the alignement of the probe card within the frame holding it in the probe station. To ensure good electrical connection between the needles and the pads, a sufficient pressure needs to be applied. Even a very small offset, especially in the vertical direction, could compromise contact, hence affecting the quality of the testing and grading results.
		
		The grading of ASICs was performed systematically, three different grades could be assigned: \textbf{A} for ASICs following nominal behaviour, \textbf{B} for ASICs with some manageable defects and \textbf{C} for unusable ASICs. The testing procedure was identical for every ASIC and was composed of the test presented below. 
		\subsubsection{Low-Voltage and High-Voltage}
			The LV is provided to the ASIC, powering up the electronics (both analog and digital). The registers of the ASICs are configured to a standard working point, hence fixing its power consumption. The current pulled on the LV power supply was previously measured for single ASICs exhibiting good behaviour and performances, the value expected is around \SI{36}{\milli\ampere}. Any much different value could hint at defects in the ASIC, compromising its operation.
			
			Ensuring that the current pulled by the ASIC on the HV power supply allows to asses whether it exhibits the electrical behaviour of a diode in reversed-bias. Once again, the reference values for good behaviour were defined doing full IV curves, at HV = \SI{-100}{\volt} and HV = \SI{200}{\volt} the current are respectively expected to be \SI{7}{\nano\ampere} and \SI{12}{\nano\ampere}. The ASIC could exhibit an Ohmic behaviour which would be symptomatic of a defect leading to a short somewhere in the ASIC, hence making it impossible to operate properly. The ASIC could also pull a much higher current than the expected one, as long as the IV trend is not Ohmic, this is a-priori not an issue unless the current is order of magnitudes higher than the expect values, this should be taken into account when grading. The results of the tests are presented in figure \ref{im:asic_stat_HV+LV}.
			\begin{figure}[h]
				\centering
				\includegraphics[width=1.0\textwidth]{files/asic_stat_HV+LV}
				\caption{Distribution of the current from the high voltage (left) and low voltage (right) power supplies, separated for the three different grades. The distribution of the HV current for \textbf{B}-grade and \textbf{C}-grade has a significant number of overflows, quoted in the legend.}
				\label{im:asic_stat_HV+LV}
			\end{figure}
			
			Only if the HV current values was in the $\mathcal{O}(10)$ nA range and the LV current was in the $\mathcal{O}(10)$ mA range, an \textbf{A}-grade would be associated to the ASIC.
			
		\subsubsection{Signal integrity}
			The quality of the signal from the data-out and clock lines is essential in determining the quality of production of an ASIC. The signals are transported by sub-LVDS lines, the nominal amplitude of the data.out is expected to be around \SI{250}{\milli\volt} such that the receiving elements on the PPB can sample properly sample the signal. 
			The phase between the clock and the data-out is also measures as it ensure that the level of the data-out signal is sampled at the correct position in time. The nominal value is expected to be, for a readout clock of \SI{25}{\mega\hertz}, around \SI{30.5}{\nano\second}, with a standard deviation of \SI{11}{\pico\second}. Only if the measured values of amplitude were \SI{200}{\milli\volt} and above, phase were within \SI{0.5}{\nano\second} of the nominal value and standard deviation not higher than \SI{20}{\pico\second}, the ASIC would receive an \textbf{A} grade. 
			Additionally, some data is collected and converted, if them aforementioned parameters are not optimal, this could lead to corrupted data, hence a failed data conversion, often leading to 100\% of the date being unusable. This is evidently a criteria automatically classifying an ASIC with a \textbf{C}-grade. 
			\begin{figure}[h]
				\centering
				\includegraphics[width=1.0\textwidth]{files/asic_stat_signalquality}
				\caption{Distribution of the amplitude of the sub-LVDS signal for the data out line (left), time difference (or phase) between the data out signal and the read-out clock line (center) and standard deviation of the time difference (right). The distributions for the three types of gardes are overlayed. }
				\label{im:asic_stat_signalquality}
			\end{figure}
		
		\subsubsection{Digital: masking and test-pulse}
			The ASIC has to be fully maskable, meaning that it could be operated silently even if at a low operating threshold, some noisy pixels are present in the matrix. A first test is performed by setting the threshold to the lowest possible values and without injecting testpulse, followed by a test with higher threshold.
			If the masking is working as expected, none of the previous scenarios described should lead to data being read-out from the ASIC. The purpose of the second test is to understand, of some pixel were found to be noisy, if a reasonably low threshold could make them silent. If all of the ASIC could be masked, the ASIC would be graded \textbf{A}, if some structures couldn't be masked, but a reasonably higher threshold could make it silent, the garde associated would be \textbf{B}, otherwise, \textbf{C}.
			In complement, some pixels in the first four rows of each column were unmasked so that a test-pulse could also be injected in a specific pattern. The test-pulse needed to be successfully injected in all SCs such that the ASIC could receive an \textbf{A}-grade.  
		       
		       
		\subsubsection{Baseline and Threshold}
			If the ASIC was fully maskable, a single pixel was unmasked and the level of the baseline of the noise is found adjusting the values for the registers associated to $threshold\_set$ and $threshold\_offset$. Once the pixel is silent, the value is measured in mV on one of the interface board for communication and powering through the probe-card. The value is expected to be around \SI{700}{\milli\volt} for an ASIC to be classified with an \textbf{A}-grade but the value is not directly used for determining the grade. 
			
			A more meaningful test to be performed is a very coarse threshold scan, performed by unmasking the entire ASIC and by collecting data for a threshold fixed at the level of the baseline of the noise and then and then add 5 and 10 DAC values to the $threshold\_set$ register and collect data again. The expected behaviour is a decrease of the amount of noisy pixel, indicating an expected spread in noise and seeing if the threshold has an effect on the matrix or not. If the behaviour was a described above, the ASIC would receive be graded with an \textbf{A}-garde. \\
			
			
			The combined results of all of the test allowed to assign a grade to each ASIC. The proportion of \textbf{A}-grade, \textbf{B}-grade and \textbf{C}-grade ASICs is presented in the figure \ref{im:asic_grade_repartition}, along with a more detailed separation within the \textbf{C}-grade ASICs according to their behaviours.
			\begin{figure}[h]
				\centering
				\includegraphics[width=1.0\textwidth]{files/asic_grade_repartition}
				\caption{Distribution of the garde given to the 1152 ASICs tested. An additional \textbf{M}-grade was added for missing ASICs, either damaged during the post processing procedures or exhibiting visual production defects (left). Distribution of the features of the \textbf{C}-grade ASICs (right), more than one feature can be exhibited by a single ASIC.}
				\label{im:asic_grade_repartition}
			\end{figure}
			
			The production yield can be estimated as the fraction of the total number of ASICs produced, classified as \textbf{A}-grade ASICs, hence 19.4\%. It is striking that almost 65\% of the ASICs were classified as \textbf{C}-grade, much lower than the conservative estimation made using the yield on the pre-production ASIC and scaling it accordingly to the respectives areas of the prototypes. Some thorough studies have been performed in order to identify the cause of the poor production yield, most of the ASIC exhibit issues linked to the masking of pixels, followed by issues in the data quality and issues linked to the response in current to the depletion voltage. 
			The \textbf{B}-grade ASICs shouldn't be accounted for in the production yield but should rather be taken into account in the defective ASICs. Since the delivery of at least 4 detector planes (288 ASICs assuming a 100\% module production yield) was essential for the performances of the entire Pre-Shower detector, a sub-classification of \textbf{B}-grade ASICs was performed in order to select the ones with least defects and gradually assemble them on modules. 
			
			 It is important to recall that designing such large ASICs is far from trivial and with the very strict deadlines on the project combined to the delays in delivery of the pre-production prototype, a production yield of almost 20\% is not to be underestimated, although not complying entirely with the requirement of the project. 
			 \clearpage
			
		
		\subsection{Module assembly}
		
		The assembly of 6 ASICs within a module is of critical importance as it may impact the overall performance of the detector. It was discussed before that the module is in charge of establishing both powering and communication between the ASICs and the readout and control system. It also couples thermally the ASICs to the cooling system, maintaining them at a constant operational temperature and provide support for alignement of the ASICs among themselves, aiming at the minimal additional dead-area in the detector, due to poor positioning of the ASICs. 
		
		The assembly procedure can be broken down into 4 successive steps for which the different trials along with measurements and the final procedure, will be presented.  
			\subsubsection{Alignement of ASICs}
			The 6 selected ASICs for assembly in a module has first to be cleaned using a plasma machine, ensuring no potential contamination by dust which could present a potential flaw in terms of mechanical integrity. The ASICs are ten positioned in a 3$\times$2 array, with the digital and I/O periphery facing outward. A plastic L-shaped tool is used to perform the alignement by blocking the ASICs in the top corner of the top assembly frame. Once the ASICs are in position, a gentle suction is performed thanks to holes drilled in the assembly frame so that the ASICs can be held in position while being manipulated. Figure \ref{im:module_assembly_0} shows the alignement of the ASICs onto the top assembly frame. 
			\begin{figure}[h]
				\centering 
				\includegraphics[width=1.0\linewidth]{files/module_assembly_0}
				\caption{Alignement of the 6 ASICS into a 3$\times$2 array using the L-shaped plastic tool (left) and later held into position using vacuum through drilled holes within the top assembly frame (right)}
				\label{im:module_assembly_0} 
			\end{figure}
			
			\subsubsection{Glueing on base-plate}
			At first, the glue was deposited using a tool instrumented with a sponge at its end, absorbing the glue and depositing it evenly on the surface of the base plate. Even if this technique provided a good and even spread of the glue, there was not control on the thickness of glue deposited, resulting in modules being assembled with an insufficient amount of glue and ASICs consequently detaching from the surface of the base plate.
			
			A refined procedure defined, implementing the deposition of a thin layer of glue being deposited on top of the base plates using \SI{150}{\micro\meter}-thick metallic frame, constraining the thickness of glue to a sufficient amount. The glue is evenly spread using a plastic spatula and a fixed number of back and forth mouvement across the length of the module. The metallic frame is shaped such that it avoids glue deposition in the interstices separating the ASIC and at the edges of the base plate. This later detail in glue deposition will be relevant for a later discussion.   
			
			The glue chosen was first an Araldyte glue mixed together with small aluminium balls to provide better thermal coupling but later was changed to Araldyte glue without any aluminium balls as it was identified with an issue in HV current causing the current to drift over time towards high values, couple order of magnitudes higher than the expected values from probe station measurements. Figure \ref{im:module_assembly_1bis} shows the ASICs held in position on the top assembly frame and the bottom assembly frame with the metallic plate mounted on the base-plate and the glue deposited on top of it. 
			\begin{figure}[h]
				\centering 
				\includegraphics[width=0.8\linewidth]{files/module_assembly_1bis}
				\caption{Preparation of glueing of the 6 ASICs on top of the base plate after the deposition of a \SI{150}{\micro\meter}-thick layer of glue across the entire base-plate surface at the exception of the edges of the modules and the interstices between the ASICs.}
				\label{im:module_assembly_1bis} 
			\end{figure}
			
			Once the glue was deposited, the top assembly frame is precisely aligned using the alignement pillars as can be seen on the right of figure \ref{im:module_assembly_1bis}. Once in contact, a pressure equivalent to a weight of \SI{500}{\gram} will be applied to ensure optimal contact between the ASIC and the glue on the base-plate. 
			At first, the vacuum was held during the drying of the glue to make sure the ASICs would not move during the process. This technique was accompanied with a series of assembly failures during which the glue would infiltrate through capillarity in-between the ASICs and on the edges of the module due to the suction power of the vacuum holding in position the ASICs. This resulted in ASICs being glued to the top assembly frame too, hence making it very dangerous to remove the frame, sometimes resulting, like in the case of module \textit{ELECT07} in the peeling of some layers of the ASICs, exposing its internal structures and damaging it to a point of no return, as shown in Figure \ref{im:module_assembly_fail}. 
			\begin{figure}[h]
				\centering 
				\includegraphics[width=1.0\linewidth]{files/module_assembly_fail}
				\caption{Assembly failure for which glue infiltrated into the interstice between ASICs at the center of the module. This resulted in ASIC 2 being pealed from its upper metal layers, unveiling the hexagonal pixels of the matrix and their front-end electronics.}
				\label{im:module_assembly_fail} 
			\end{figure}
			
			The final procedure was adapted and the vacuum was removed while the glue was drying. Additional care was taken by limiting the deposition of glue in the regions below the interstices between ASICs to disfavour even more the infiltration of glue. A deviation of the ASICs from the aligned position was expected but measured to be below \SI{100}{\micro\meter} in the worst cases. Evidently, any loss at the level of the modules, even of a single ASIC, becomes a 6-ASIC loss as it is impossible to substitute a faulty ASIC once assembled on a module. 
			
			\subsubsection{Glueing of module-flex}
			The module-flex is glued on top of the ASICs following a similar procedure. The module flex is positioned in a second top assembly frame, aligned through the use of a precise plastic frame, using the two holes at the center of the left and right edges as reference, and held in position with vacuum. 
			
			At first, the glue was deposited homogeneously using the tool instrumented with the sponge at its end but this first procedure lead to some electrical issues. Usually, a module is expected to pull current on the HV power supply at a level compatible the sum of the individual currents for every ASIC, i.e. in the order of \SI{45}{\nano\ampere} for a depletion voltage of \SI{120}{\volt}. It was observed that some modules would pull currents at the level of \SI{1.5}{\micro\ampere}, only at a depletion voltage of \SI{2.4}{\volt}. This enormous current consumption was also associated to the unmasking of a some specific SP in a specific SC in a specific ASIC. 
			
			After reconstructing the position of the pixels responsible for this problem, it was identified to coincide with the position of the HV via in the module-flex, crossing all of its layer from the top surface to thee bottom, distributing the HV to all ASICs. This via hence needed to be insulated from the surface of the ASIC in order to avoid electrical coupling between the ASICs and the HV-via. A \SI{50}{\micro\meter}.thick layer of transparent kapton-tape was added at the location of the via and equivalently at the other side of the module to avoid mechanical constraints on the module-flex. Figure \ref{im:module_assembly_fix0} shows the glue and kapton tape applied at the bottom of the module-flex.
			\begin{figure}[h]
				\centering 
				\includegraphics[width=0.7\linewidth]{files/module_assembly_fix0}
				\caption{Deposition of glue on the bottom of the module flex. On the left side, a circular HV-via goes from the top of the module-flex to the bottom, hence impacting the stability of the ASIC located underneath. A \SI{50}{\micro\meter}-thick kapton tape layer is added to electrically insulate the HV-via. An equivalent thickness of kapton tape is added at the opposite side. }
				\label{im:module_assembly_fix0} 
			\end{figure}
			
			The module flex is then aligned in position onto the ASICs using the same alignement pillars as previously, hence ensuring optimal mechanical alignement between the layers. A pressure equivalent to the weight of \SI{500}{\gram} is applied for optimal glueing. 
			Once the glue has dried, the top assembly frame can be removed and the mechanical part of the assembly procedure is finished.
			 
			\subsubsection{Wire-bonding}
			The final step of the module assembly is the electrical connection between the module-flex and the pads on the ASICs for powering and I/O. As previously mentioned, each ASIC has a total of 134 pads requiring wire-bonding, so a total of 804 wire-bonds at the level of the module. The wire bonding is, after several manual attempts to define the procedure, automatised so that, without the need of any intervention, the wire-bonding of an entire module can be performed in under \SI{20}{\minute}. The presence of a technician is fondamental during this procedure as it requires extreme attention to control the quality of the wire-bonds. Some are specifically difficult to do and have to be manually performed in some cases, requiring great precision and care to avoid damaging neighbouring wire-bonds or components. The wire-bonding machines produces the electrical connection through supersonic waves, melting the tip of the very thin wire onto the pads on both ends through the application of a precisely calibrated pressure. Figure \ref{im:bonding} shows a module after wire-bonding was performed. 
			
			\begin{figure}[h]
				\centering 
				\includegraphics[width=0.7\linewidth]{files/bonding}
				\caption{Wire-bonding between a module-flex and the pads of the ASIC located underneath. Each ASIC requires 134 wire-bonds to be made between its powering and I/O pads and the ones on the module-flex. The image shows in focus the wire-bonds for ASIC 3, highlighting how precise and meticulous is the procedure.}
				\label{im:bonding} 
			\end{figure}
			
			A final visual inspection of the glueing of the ASICs to make sure no damage was caused during handling or wire-bonding, as well as a final inspection of the wire-bonds themselves are performed. A transparent plastic protection cap is ultimately glued on top of the module-flex, providing protection for the wire-bonds, any un-noticed contact between wire-bonds could lead to a short between different lines, eventually damaging one or more ASICs. 

		\subsection{Characterisation set-up}
		The modules must undergo an extensive testing procedure in order to be qualified for installation into a detector plane and later into the entire FASER Pre-Shower detector. The test set-ups were installed in the UniGe clean room so that both assembly and qualification happened in the same location, hence accelerating the entire procedure. In order to match the strict timelines of the project, two identical test set-ups were assembled in parallel in order to reach at peak productivity, a qualification rate of one module per day. 
		
		Figure \ref{im:test_setup} shows the dual configuration set-up used for qualification of modules and a single test set-up can be broken-down into the elements listed below. 
		
		\begin{figure}[h]
			\centering 
			\includegraphics[width=0.9\linewidth]{files/test_setup}
			\caption{Dual test set-up configuration for the qualification of modules pre-assembly into a detector plane. The different components of the set-up are labeled. A light-tight cloth is put on top of the two modules are they are sensible to light are meant to be operated in the dark. A copper ground connection is made between the LB+PPB system to the optical table and the modules are grounded too.}
			\label{im:test_setup} 
		\end{figure}
		  
			\subsubsection{LV and HV power supplies} 
			The LV power supply needs to provide a \SI{12}{\volt} output with a current compliance set at \SI{0.6}{\ampere} for the LB and a \SI{12}{\volt} output with a current compliance set at \SI{0.2}{\ampere} to the PPB, hence powering the modules. The HV power supply needs ti provide a \SI{120}{\volt} output with a current compliance set at \SI{1}{\micro\ampere}. The current compliances need to be strict as they have to prevent any damage to components both on board and in the ASIC itself.
			
			\subsubsection{Oscilloscope} 
			Used mainly for debugging purposes to monitor the clock lines for readout and configurations, the SPI lines to check wether configurations are sent or not and the data-out lines to check wether or not the modules sends data in output but also its quality.
			
			\subsubsection{LB and PPB} 
			The PPB has 6 different slots on which modules can be connected, each group of two slots share the same LV powering lines while the HV is shared among all slots and the communication lines are unique per slot. Only one module was connected per test set-up as no gain in time would have been obtained due to the nature of the test procedure and could have eventually resulted in slower qualification due to a higher overall level of parasitic noise.
			The LB is interfaced to the PPB on one side and to the user on the other side through a 1 Gbps ethernet link. \\
			Both the LB and PPB are grounded to the optical table for reference, itself grounded to the general reference of the laboratory. 
			
			\subsubsection{Computer} 
			The ASIC in the modules are controlled through a series of commands it receives from the FPGA on the LB through the Slow-Control procedure described in \note{section ref}. The computer is also used to flash the FirmWare (FW) into the FPGA. The computer needs to be able to run the GUI designed specifically for the UniGe GPIO Board, of which the LB is a derivation. 
			
			\subsubsection{Module}
			A single modules is mounted per test set-up. The interface with the PPB is performed using a communication and powering pigtail. According to the connection slot used on the PPB, the length of the pigtails have to be adjusted. The cable length can have an impact on the performance of the ASIC, especially in terms of LV and HV effectively provided to the module, a non negligible tension drop can be measured for the furthermost slots on the PPB. The frame holding the module is connected to the optical table through a copper grounding line to impose a unique reference for the entire system. The modules are also shielded from external light thanks to a light-tight cloth. 
			Figure \ref{im:test_setup_zoom} shows a more detailed view of the set-up and what was hidden in figure \ref{im:test_setup} by the cloth.
			
			\begin{figure}[h]
				\centering 
				\includegraphics[width=0.9\linewidth]{files/test_setup_zoom}
				\caption{Close-up view of the module connected to the PPB through the powering and communication pigtails (left) and of the connection of the pigtails onto the module (right). The modules are held into an aluminium frame for safer handling and an additional, more resistant protection cover is mounted on the aluminium frame. }
				\label{im:test_setup_zoom} 
			\end{figure}
			
			
			\subsubsection{Cooling and Dry-Air}
			The frame holding the modules to connected to a cooling plate with a closed water loop for which the temperature is controlled by a chiller and fixed at \SI{15}{\celsius} to mimic real operating conditions. 
			
			\begin{figure}[h]
				\centering 
				\includegraphics[width=1.0\linewidth]{files/HVcurrent_dryair}
				\caption{HV current pulled by a module as a function of time. The current rises and fluctuates until the dry air is injected, reducing the current to the values expected from the direct sum of the current consumptions for each ASIC.}
				\label{im:HVcurrent_dryair} 
			\end{figure}
			
			During the test of the first produced modules, a drift towards high values of HV current was observed over time, but wasn't noticed at the level of the single ASIC testing. The current would ultimately reach saturation at values in the order of several $\mu$A of current which is a priori not problematic for a system of 6 ASICs but is symptomatic of an element in the system composed by the module, its frame and the cooling plate. 
			At first glance, the problem was associated to a coupling between the base plate of the module and its holding frame due to the aluminium balls contained in the glue. The problem persisted until some dry-air was flushed within the volume between the aluminium frame and the transparent module cover (see figure \ref{im:test_setup_zoom}). The glue being hydrophilic, it needs to be dried not to become conductive, hence creating a conductive path between the ASICs and the base plate of the module. The result of the injection of dry-air on the current can be seen in figure \ref{im:HVcurrent_dryair}.

		\subsection{Front-End Working Points}
		The front-end electronics of the ASIC have a default configuration that is imposed when the ASIC is powered-up. Indeed, if not fixed, the values of the registers can be random when first powering the ASIC. This could lead to unwanted configurations of the front-end electronics, hence possibly damaging the ASIC by providing a too high current to some of the elements in the circuitry.
		
		\begin{table}[h]
			\centering
			\renewcommand{\arraystretch}{1.2}
			\begin{tabular}{|l|c|c|c|c|}
			\hline
			\textbf{Register name} & \textbf{Address} & \textbf{Default} & \textbf{WP2} & \textbf{WP3} \\
			\hline
			bias$\_$preamp        & 0b000101 (5)   & 20  & 10  & 10 					\\ \hline
			bias$\_$feedback      & 0b000110 (6)   & 40  & 5   & 25 					\\ \hline
			bias$\_$disc          & 0b000111 (7)   & 1   & \multicolumn{2}{c|}{1} 	\\ \hline
			bias$\_$idle          & 0b001000 (8)   & 200 & \multicolumn{2}{c|}{200} \\ \hline
			bias$\_$LVDS          & 0b001001 (9)   & 100 & \multicolumn{2}{c|}{100} \\ \hline
			bias$\_$load          & 0b001010 (10)  & 5   & var & var 				\\ \hline
			bandgap$\_$config     & 0b001011 (11)  & 64  & \multicolumn{2}{c|}{64} 	\\ \hline
			bias$\_$testpulse     & 0b001100 (12)  & 0   & \multicolumn{2}{c|}{0} 	\\ \hline
			threshold$\_$set      & 0b001101 (13)  & 0   & var & var 				\\ \hline
			threshold$\_$offset   & 0b011101 (29)  & 0   & var & var 				\\ \hline
			bias$\_$pixel         & 0b010011 (19)  & 255 & \multicolumn{2}{c|}{255} \\ \hline
			testpulse$\_$delay    & 0b110010 (50)  & 0   & \multicolumn{2}{c|}{0} 	\\ \hline
			config$\_$global      & 0b011110 (30)  & 224 & \multicolumn{2}{c|}{254} \\ \hline
			readout$\_$config     & 0b011111 (31)  & 255 & \multicolumn{2}{c|}{255} \\ \hline
			TDC$\_$config         & 0b000011 (3)   & 100 & \multicolumn{2}{c|}{100} \\ \hline
			\end{tabular}
			\caption{Definition of the default configuration as well as the two operation WP. Both have similar configurations DAC-codes for many registers, at the exception of the \textit{bias$\_$preamp}, \textit{bias$\_$feedback}, \textit{bias$\_$load}, \textit{threshold$\_$set} and \textit{threshold$\_$offset} which can vary on an ASIC to ASIC basis.}
			\label{tab:frontend_wp}
		\end{table}
		
		It was observed during simulations that the charge distribution for the different detector planes of the Pre-Shower detector were quite different has shown by figure \ref{im:EM_shower_dev}. In order to limit the amount of data sent in the output, it is not necessary to detect on the last planes, the smallest charges as these layers serve as confirmation for the position of the center of the EM shower when reconstructing the photons' positions in the Pre-Shower. 
		Consequently, two Working Points (WP) of the front-end electronics, commonly named WP2 and WP3, were designed to have a sensitivity starting respectively at a deposited charge of \SI{1}{\femto\coulomb}, and  \SI{4}{\femto\coulomb}. Table \ref{tab:frontend_wp} summarises the different values of the registers for the default configurations and both WP2 and WP3. 
		
	
		The registers responsible for the sensitivity to charge of the ASIC are the \textit{bias$\_$preamp} and \textit{bias$\_$feedback}. The charge injected for specific values of \textit{bias$\_$testpulse} are known from simulations and can be verified with radioactive source measurements. Since there is mismatch from pixel to pixel on the amount of charge injected for the same DAC-code. For this reason, a sample of $\mathcal{O}(10)$ pixels were pulsed accros the matrix to ensure that a the minimum charge could be observed for all of them, with a discrimination threshold high enough to make the ASIC silent but low enough to not compromise the sensitivity. The optimal threshold value will later be defined ASIC by ASIC by a dedicated calibration.
		
		 Moreover, the \textit{bias$\_$load} register also has an impact on the sensitivity of the ASIC as it defines the current charging the analog memories, hence the sensitivity to the smallest charge the ADC can be sensible to. This parameter will also need to be tuned in a dedicated calibration. 
	
		The modules require calibration for both Working Points of the front-end electronics as the discrimination threshold and pixels to be masked, as well as the response to deposited charge will be different. It is not to be excluded that event with the free parameters available to tune the sensitivity of the ASIC, that a modification of the DAC-codes of \textit{bias$\_$preamp} and \textit{bias$\_$feedback} is required for some peculiar ASICs. 
		
		
		\subsection{Threshold Calibration}
		The first and fondamental step in calibration of an ASIC, once its front-end electronics WP is defined, is finding its discrimination threshold. The value has to be carefully chosen far enough from the noise baseline so that the ASIC would not be triggered by spurious noise events, but close enough so that the sensitivity is not considerably reduced. Any pixel active at the discrimination threshold chosen will need to ne masked, hence increasing the effective fraction of dead-area in the ASIC. 
		
%			\subsubsection{Procedure}
		    A threshold scan is usually performed in order to obtain as a function of the discrimination threshold, the amount of activated pixels, i.e. scanning the distribution of the level of the baseline of the noise and its spread for all the pixels collectively. The result obtained is a so called S-curve, which helps identifying the discrimination threshold per ASIC as well as quantifying the standard deviation of the noise at the ASIC level. 
		    
		    The procedure developed for the threshold scan of the FASER production ASIC on module is the result of the optimisation of precision of the result and time available to test each module. For this reason, the threshold scan were performed twice, once on a large range of values with coarse steps and a second time with a smaller range and smaller steps, defined by the results of the first coarse scan. 
		    Each scan is performed the exact same way at the lower level and can be broken down as follows: 
		    
		    \begin{itemize}
		    	\item Configuration for every ASIC in the module of all the registers to one of the two front-end electronics WP. The \textit{threshold$\_$set} and \textit{threshold$\_$offset} registers are set to a default DAC-code of 100 and 0 respectively. 
		    	\item Masking the entire matrix of every ASIC in the module. 
		    	\item Loop over the ASICs to be scanned for this iteration. 
		    	\begin{itemize}
		    		\item Disabling readout at the module level for all but the ASIC in the loop. 
		    		\item Reading from configuration file, the lowest and highest threshold values to be scanned through the DAC-codes of the \textit{threshold$\_$set} and \textit{threshold$\_$offset} registers. The step size in DAC-codes units between each tested value is also read from the configuration file.
		    		\item Loop over the threshold values 
		    			\begin{itemize}
		    				\item Loop over the SC of the ASIC.
		    				\begin{itemize}
		    					\item Unmasking every pixel in the matrix corresponding to the SC.
		    					\item Acquiring data. If the transferred amount of data is lower than 1000 kB, the script waits 100 ms and then proceeds to check again the amount of transferred data. This check is performed 100 times so that a maximum of 10 seconds is waited before ending the data acquisition. 
		    					\item Masking every pixel of the matrix corresponding to the SC. 
		    				\end{itemize}
		    			\end{itemize}
		    		\item Entirely mask the matrix of the ASIC tested (redundancy for safety).
		    	\end{itemize}
		    	\item Conversion of the data for every iteration and extraction of the address of each active pixel. The list of pixels are then merged together to have one file per ASIC per threshold value. 
		    \end{itemize}
		    
%		    \subsubsection{Results}
		    The typical result obtained after a coarse and fine threshold scan can be combined, and is given in figure \ref{im:Scurve_ELECT03} for module ELECT03 operated at WP2 and WP3. 
		    
		    \begin{figure}[h]
				\centering 
				\includegraphics[width=0.95\linewidth]{files/Scurve_ELECT03_WP2}
				\includegraphics[width=0.95\linewidth]{files/Scurve_ELECT03_WP3}
				\caption{Result of the combination of a fine and coarse threshold scan for module ELECT03 operated at WP2 (top) and WP3 (bottom). The vertical axis shows the log$_{10}$ of the number of active pixels as a function of the discrimination threshold in units of DAC-codes.}
				\label{im:Scurve_ELECT03} 
			\end{figure}
		
			Three different regimes can be identified for each S-curve, going from low to high threshold values: the \textit{noisy regime} in which he S-curve reaches the maximum possible number of activated pixels in the matrix, the \textit{transition regime}, in which the number of pixels with a baseline level of the noise over threshold gradually decreases and the \textit{stable regime} in which the S-curve reaches its minimum value and the number of active pixels reached a minimum. 

			The threshold is extracted manually as the lowest DAC-code for which the S-curve is in the stable regime. One has to pay attention to the slope of the S-curve near the chosen threshold, the situation in which a small variation of the noise in the system would increase drastically the number of active pixels at the discrimination threshold is to be avoided. Moreover, for very high values of discrimination threshold, the number of active pixels is expected to reach 0, although this is not necessarily wanted. The choice of the discrimination threshold is always a compromise between sensitivity and amount of pixels needed to be masked. The distribution of the discrimination threshold selected for WP2 and WP3 is given in figure \ref{im:thr_scan_bothWP}.
			
			\begin{figure}[h]
				\centering 
				\includegraphics[width=0.85\linewidth]{files/thr_scan_bothWP_allasic}
				\caption{Distribution of the discrimination threshold selected for all ASICs mounted on module for WP2 (left) and WP3 (right). }
				\label{im:thr_scan_bothWP} 
			\end{figure}
			
			The mean values of discrimination threshold are compatible between the two WP with a value of \textit{threshold$\_$set} = 117 DAC. The dispersion of the threshold is also quite similar between WP2 and WP3 with a standard deviation respectively of $\sigma_{\text{thr}}$ = 38.5 DAC and $\sigma_{\text{thr}}$ = 37.6 DAC. This results highlights that within the level of sensitivity of the threshold (i.e. its LSB), the change in noise of the front-end electronics in average did not vary significantly between the two WP. 
			
			\begin{figure}[h]
				\centering 
				\includegraphics[width=0.45\linewidth]{files/thr_scan_WP2_perasic}
				\includegraphics[width=0.45\linewidth]{files/thr_scan_WP3_perasic}
				\caption{Distribution of the discrimination threshold selected per ASIC position in the module for WP2 (left) and WP3 (right). }
				\label{im:thr_scan_bothWP} 
			\end{figure}
			
			A more detailed study can be performed by classifying the ASICs according to their position within the module, the results are presented in figure \ref{im:thr_scan_bothWP} and the results of the mean and standard deviation for each threshold distribution is given in table \ref{tab:threshold_perasic_bothWP}
			
			\begin{table}[h]
    			\centering
    			\renewcommand{\arraystretch}{1.15}
    			\begin{tabular}{|c||c||c|}
        			\hline
        			& \textbf{WP 2} & \textbf{WP 3} \\
        			\hline
        			\textbf{ASIC} & $(\mu, \sigma)$ [DAC] & $(\mu, \sigma)$ [DAC] \\ \hline
        			0 & (113, 38) & (113, 36) \\ \hline
        			1 & (123, 32) & (121, 32) \\ \hline
        			2 & (123, 32) & (122, 31) \\ \hline
        			3 & (110, 38) & (112, 37) \\ \hline
        			4 & (120, 41) & (119, 43) \\ \hline
        			5 & (107, 46) & (109, 43) \\ \hline
    			\end{tabular}
    			\caption{Mean and standard deviation of threshold distributions per ASIC, for WP2 and WP3, shown as $(\mu, \sigma)$ [DAC].}
    			\label{tab:threshold_perasic_bothWP}
			\end{table}
			
			It is interesting to note the correlation between the location of the ASIC in the module and the mean discrimination threshold. 
			
			It seems to be like the ASICs position in the top left, bottom center and bottom right have a lower discrimination threshold can the other ASICs, by 8 DAC-codes in average . Giving an interpretation to this result is difficult as there are many possible explanation for this additional noise felt by the ASIC in specific positions in the module. \note{ too little statistic, change conclusion }

		 	Each S-curve can be fitted with a custom Cumulative Distribution Function (CDF) of a Gaussian distribution, i.e. the function usually used to described the noise distribution, so that: 
			\begin{equation}
				S(x) = B + A \cdot \left[1 - \mathrm{erf}\left(\frac{x - \mu}{\sqrt{2} \sigma}\right)\right]
			\end{equation}
			where the parameter $B$ and $A$ are respectively the baseline of the S-curve and its maximum and $\mu$ and $\sigma$ respectively the mean and standard deviation of the Gaussian distribution of the noise. The distribution of the mean and standard deviation of the noise, $\mu_{\text{V}}$ and \noise can be built by extracting the fit parameters for each S-curve. The results are presented for WP2 and WP3 in figure \ref{im:noise_distribution_bothWP}.
		
		 	\begin{figure}[h]
				\centering 
				\includegraphics[width=0.49\linewidth]{files/baseline_distribution_bothWP}
				\includegraphics[width=0.49\linewidth]{files/noise_distribution_bothWP}
				\caption{Distribution of the mean and standard deviation of the noise \noise for WP2 (left) and WP3 (right). The units for both $\mu_{\text{V}}$ and \noise are given in DAC-codes.}
				\label{im:noise_distribution_bothWP} 
			\end{figure}
			
			
			A calibration of the LSB of the register \textit{threshold$\_$set} would be required in order to extract the conversion factor between DAC-code values and the equivalent voltage threshold. From simulation, the LSB, with at least a 20\% mismatch, was extracted to be \SI{3}{\milli\volt} per DAC in its linear regime so that the mean of the noise for W2 and WP3 are respectively \SI{326}{\milli\volt} and \SI{308}{\milli\volt} while the standard deviation of the noise for WP2 and WP3 are respectively \SI{8.5}{\milli\volt} and \SI{9.5}{\milli\volt}. This result is interesting as it shows that the baseline of the noise has decreased from WP2 to WP3 while the standard deviation of the noise has increased. 
			This also confirms that in average, the change in noise from one WP to the other is below the sensitivity of the LSB of the threshold registers. A further calibration to extract the conversion factor between the level of the output signal and the input charge of the amplifier, would enable the computation of the ENC of the front-end.  
		
			Finally, it is interesting to look at the correlation between the discrimination threshold selected and the standard deviation of the noise extracted from the S-curve, the results are presented in figure \ref{im:noise_vs_thr_bothWP}.
			
			\begin{figure}[h]
				\centering 
				\includegraphics[width=0.49\linewidth]{files/noise_vs_thr_WP2}
				\includegraphics[width=0.49\linewidth]{files/noise_vs_thr_WP3}
				\caption{Distribution of the noise in DAC-codes values as a function of the discrimination threshold in DAC-code values, for WP2 (left) and WP3 (right). The is a linear trend showing a dependence between the two variables.} 
			\end{figure}
			
			The dependence of the discrimination threshold selected as a function of the noise extracted from the S-curves seems to follow a linear trend. \note{This is explained by the linearity of the LSB that changes as a function of the DAC-code you set}
		
		\subsection{Noise Calibrations}
		Once the discrimination threshold is fixed, one can move to the second step in ASIC calibration, i.e. the identification of the noisy pixels to be masked in order to operate the ASIC in stable conditions. Since noise is a stochastic process, the identification of noisy pixels has to be performed on a sufficiently long interval of time in order to reduce as much as possible the Noise Hit Rate (NHR). 
		
		\subsubsection{Procedure}
		A noise scan is usually performed in order to obtain the list of pixels to be masked for a stable operation of the ASIC. The procedure developed for the noise scan of the FASER production ASIC on module is again the results of the optimisation of the accuracy of the result and time available to test each module. For this reason, the noise scans were performed three times, every time with a different acquisition time per ASIC in order to identify even the pixels rather fare from the baseline but still exhibit excess noise above the discrimination threshold. From one iteration to the other, the list of noisy pixel was extracted and masked. Indeed, some pixel could be so noisy that they could saturate the read-out hence making it almost impossible to identify less noisy pixels. Each noise scan with different time is performed in the exact same way at the lower level and can be broken down as follows: 
		
		\begin{itemize}
			\item Definition of the \textit{acquisition time}.
			\item Configuration for every ASIC in the module of all the registers to one of the two front-end WPs. The values for the threshold associated registers are the one found previously. 
			\item Reading of the mask configuration for every ASIC in the module. If a previous iteration was performed, the list is made of the addresses of the noisy pixels found, otherwise it is empty. 
			\item Masking the entire matrix of every ASIC in the module. 
			\item Loop over the ASICs to be scanned for this iteration.
			\begin{itemize}
				\item Disabling readout at the module level for all but the ASIC in the loop.
				\item Loop over the SC.
				\begin{itemize}
					\item Mask the SC accordingly to the pixels addresses found in the mask configuration.
					\item Acquiring data. if the transferred amount of data is lower than 1000 kB, the script waits 5$\times$ \textit{acquisition time} ms and proceeds to check again the transferred amount of data. This is repeated 200 times, in total, the amount of time waited is equal to \textit{acquisition time}. Once one or the other condition is reached, the data acquisition is interrupted.
					\item Masking every pixel in the matrix corresponding to the SC.  
				\end{itemize}
				\item Disabling readout for the ASIC in the loop.
			\end{itemize}
			\item Conversion of the data for every SC into a single file per ASIC. The unique list of pixels to be masked at the next iteration is added to the mask configuration. 
		\end{itemize}
		
		
		\subsubsection{Results}
		
		The typical result obtained after the combination of three separate noise scans with \textit{acquisition time} of \SI{1}{\second}, \SI{10}{\second} and \SI{60}{\second} can be combined and is given in figure \ref{im:noisescan_typical_result} for module ELECT03 operated at WP2 and WP3. 
		
		\begin{figure}[h]
			\centering 
			\includegraphics[width=1.0\linewidth]{files/noisescan_typical_result}
			\caption{Result of the combination of three consecutive iteration of noise scan with \textit{acquisition time} of \SI{1}{\second}, \SI{10}{\second} and \SI{60}{\second}. The image shows the distribution of the pixels to be masked for the 6 ASICs in module ELECT03.}
			\label{im:noisescan_typical_result} 
		\end{figure}
		
		This example is particularly useful as it highlight different types of noisy structures an ASIC can have in its matrix. The first and most striking one is the entire SP located in ASIC 3 (bottom center position), which could result noisier than other areas because of a mismatch in the local biasing circuit or in the ADC being too more sensitive than others. The second most striking structure is the bottom edge of ASIC 1 (bottom left), which could result noisier because \note{insert explanation here, ask LORENZO}. Finally some vertical structures are also present \note{ask LORENZO for explanation}.
		
		The number of pixels to be masked is a key indicator of the performance of an ASIC as it represents the additional amount of dead-area in an ASIC's matrix. This number can be extracted for every ASIC in every module and its distribution is given for WP2 and WP3 in figure \ref{im:noise_scan_bothWP_allasics}
		
		\begin{figure}[h]
			\centering 
			\includegraphics[width=1.0\linewidth]{files/noise_scan_bothWP_allasics}
			\caption{Distribution of the number of active pixels (i.e. pixels to be masked) per ASIC for WP2 (left) and WP3 (right). The distribution show that very rarely, more than 1\% of the ASIC's matrix need to be masked. }
			\label{im:noise_scan_bothWP_allasics} 
		\end{figure}
		
		The number of pixels to be masked rarely get past the point in which more than 1\% (~270 pixels) of the ASIC's matrix need to be masked. As a matter of fact the average number of noisy pixels for WP2 and WP3 are respectively 68 pixels and 63 pixels, with a standard deviation respectively of 97 pixels and 101 pixels. It is interesting to note that for WP3, ASICs require in general less noisy pixels to be masked than for WP2. \note{Ask Lorenzo for explanation.}
		
		A more detailed study can be performed by classifying the ASICs according to their position within the module, the results are presented in figure \ref{im:} and the results of the mean and standard deviation for each distribution is given in table \ref{tab:noise_perasic_bothWP}
% TODO: Add line equivalent at 1% of problematic ASICs in plot, also make the x axis go until 26600. 
		\begin{figure}[h]
			\centering 
			\includegraphics[width=0.49\linewidth]{files/noise_scan_WP2_perasic}
			\includegraphics[width=0.49\linewidth]{files/noise_scan_WP3_perasic}
			\caption{Distribution of the discrimination threshold selected per ASIC position in the module for WP2 (left) and WP3 (right). }
			\label{im:noise_scan_bothWP} 
		\end{figure}
		
% TODO verify the values of mean and sigma, maybe restrict in range until 500 
		\begin{table}[h]
    			\centering
    			\renewcommand{\arraystretch}{1.15}
    			\begin{tabular}{|c||c||c|}
        			\hline
        			& \textbf{WP 2} & \textbf{WP 3} \\
        			\hline
        			\textbf{ASIC} & $(\mu, \sigma)$ [pixels] & $(\mu, \sigma)$ [pixels] \\ \hline
        			0 & (60, 71)  & (73, 114) \\ \hline
        			1 & (68, 97)  & (66, 103) \\ \hline
        			2 & (82, 123) & (48, 88) \\ \hline
        			3 & (50, 80)  & (48, 93) \\ \hline
        			4 & (66, 103) & (58, 109) \\ \hline
        			5 & (83, 92)  & (72, 84) \\ \hline
    			\end{tabular}
    			\caption{Mean and standard deviation of number of pixels to be masked per ASIC, for WP2 and WP3, shown as $(\mu, \sigma)$ [pixels].}
    			\label{tab:noise_perasic_bothWP}
			\end{table}
			
			There is no clear trend to be observed as a function of the position of the ASIC within the module. The number of noisy pixels is of course also linked to threshold. It was previously said that the threshold needs to be taken in the stable regime of the S-curve but as close as possible to the beginning of the transition regime to find the best compromise between sensitivity and amount of pixels to be masked. In average, not more than 0.35\% of the matrix has to be masked at the level of the ASIC. 
			
			Furthermore, it is interesting to study the position within the ASIC of the pixels to be masked, the results are presented for WP2 and WP3 in figure \ref{im:noisy_pixelmap_ASIC}.
			
			\begin{figure}[h]
				\centering 
				\includegraphics[width=1.0\linewidth]{files/noisy_pixelmap_ASIC}
				\caption{Map of the position of the noisy pixel to be masked at the level of the ASIC for WP2 (left) and WP3 (right). The colour scale start at 2 so that only pixel that have to be masked more than once are highlighted.} 
				\label{im:noisy_pixelmap_ASIC}
			\end{figure}
			
			The results show effectively that more pixels have to be masked for WP2 than WP3 as anticipated from the distributions of number of pixels to be masked per WP. Moreover, it is quite clear, especially in the case of WP3, that there seems to be more noisy pixels at the center of the ASIC, and that only one SC out of two seems to suffer from higher noise. This result is interesting as it probably highlights that the discrimination threshold might be too hard for some SC but too loose for some of the others. The interpretation of the higher concentration of noisy pixel at the center of the ASIC can be understood by looking how the threshold level is distributed within the ASIC. Indeed, the threshold level is generated at the edges of the ASIC, the effective level that is propagated to the central SC suffers from the resistance of the lines transporting the signal, effectively reducing its value. \note{The threshold is generated in the bottom left corner but then distributed from the top, central columns still are more sensible to dispersion}
			
			To get more insight into the situation at the level of the module, it is possible to build the same distribution but positioning the ASIC at their location in the module, the results are given for both WP in figure \ref{im:noisy_pixelmap_module}.
			
			 \begin{figure}[h]
				\centering 
				\includegraphics[width=1.0\linewidth]{files/noisy_pixelmap_module}
				\caption{Map of the position of the noisy pixel to be masked at the level of the ASIC for WP2 (left) and WP3 (right). The colour scale start at 2 so that only pixel that have to be masked more than once are highlighted.} 
				\label{im:noisy_pixelmap_module}
			\end{figure}
			
			The situation becomes more interesting as it seems to be that a specific location with the module, bottom right corner, shows a higher number of noisy pixels both for WP2 and WP3. Moreover, the situation is even worst for a single SC within ASIC 5. The attentive reader would have noticed that this is not the first time such a problem is mentioned. Indeed, while testing the assembly procedure, it was discovered that the HV via in the module-flex, located on top of ASIC 5 had the effect of making the ASIC pull particularly high HV current when specific pixels in a specific SC were unmasked. The SC 7 correspond to the position on the module-flex of the HV via and seems to be a more noisy SC, this could indicate that even if the problem was mitigated by adding a \SI{50}{\micro\meter}-thick layer of kapton tape below the position of the HV via, there still seems to be a problem associated to it, resulting in higher number of noisy pixels around its location.     
			
			
		
		
		
		
		
		
% TODO: Map at the level of the ASICs of the problematic pixels. done
% TODO: Map at the level of the module of the problematic pixels. done
% TODO: On the noise scan, see if there is a correlation with the position == The Maps. done 

		
		\subsection{Charge calibration \textcolor{red}{ 4 pages}} 
% TODO: Explain the procedure  
		
		\subsection{Plane assembly \textcolor{red}{ 3 pages}}
% TODO: Show the mapping of the planes and how it was made from the testpulse scan plots and using the thresholds 
% TODO: Show the noisy pixels maps for the full plane. 	
	
	
	
	
	
	\clearpage
	\section{Trigger and Data Acquisition: TDAQ \textcolor{blue}{ 9 pages}}
		\subsection{Readout\textcolor{red}{ 2 pages}}
% TODO: Done already in previous chapter, is it enough ?
		\subsection{Data conversion \textcolor{red}{ 3 pages}}
% TODO: Not sure what to say here expect how the events are structured ? Maybe should go in the previous chapter
		\subsection{Data corruption and solutions  \textcolor{red}{ 3 pages}}
% TODO: Show the studies done with the termination of the sub-LVDS lines at 100 Ohm nominal and how it changed. 
		
	
	
	
	\clearpage
	\section{Testbeam campaign for detector commissioning}
% TODO: Describe layout with powering, trigger etc, 
% TODO: Show the beam profil, meaning that the full system was able to acquire this much data. 
% TODO - Analysis: align the preshower planes among themselves. Use the tracker information to extrapolate the position on the PreShower and clean the event displays and the future analysis I do. 
% TODO - Analysis: Show the deposited charge per plane and show there is a correlation on the position of the hits per plane. 
% TODO - Analysis: Show the energy deposited per plane per event as a function of the beam energy. 
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
%	\section{Pre-Production ASIC characterization \textcolor{blue}{ 6 pages}}
%		\subsection{ASIC description \textcolor{red}{ 3 pages}} 
%		\subsection{Time-Over-Threshold mismatch \textcolor{red}{ 3 pages}}